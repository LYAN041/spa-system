const [dataTable, setDataTable] = useState()

  const renderSwitch = (category) => {

    let dataFilter

    switch (category) {

      case "limpeza":
         dataFilter = items.filter(item => item.category === "limpeza");
         setDataTable([dataFilter])
        break;

        case "velas":
         dataFilter = items.filter(item => item.category === "velas");
         setDataTable([dataFilter])
        break;

        case "cozinha":
         dataFilter = items.filter(item => item.category === "cozinha");
         setDataTable([dataFilter])
        break;

        case "massagem":
         dataFilter = items.filter(item => item.category === "massagem");
         setDataTable([dataFilter])
        break;

        case "dayspa":
         dataFilter = items.filter(item => item.category === "dayspa");
         setDataTable([dataFilter])
        break;

        case "outros":
         dataFilter = items.filter(item => item.category === "outros");
         setDataTable([dataFilter])
        break;
    
      default:
        break;
    }

  }
